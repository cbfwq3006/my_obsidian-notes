# Codex 知识库工作规则

## 定位

这是 Obsidian + Claudian + Codex 共用的个人知识库。Codex 进入本库工作时，先遵守本文件，再结合 `CLAUDE.md` 和 `wiki/index.md` 判断具体操作。

## 工具分工

- Obsidian：阅读、双链、图谱、日常笔记维护。
- Claudian：在 Obsidian 内对单篇笔记做快速问答、润色、改写。
- Codex：跨文件分析、批量整理、资料摄取、生成项目材料、维护 `wiki/` 结构。

## 目录约定

- `raw/`：原始资料层，默认只读，不覆盖、不改写；除非用户明确要求整理原文。
- `wiki/`：稳定知识层，按 `CLAUDE.md` 的 LLM Wiki Schema 维护。
- `wiki/来源/`：每个 raw 来源对应一个来源摘要页。
- `wiki/概念/`：跨来源沉淀出的可复用概念。
- `wiki/实体/`：人物、组织、工具、产品等实体。
- `wiki/对比/`：跨来源综合、比较、专题分析。
- `01 Inbox/`：临时收集箱，未处理材料先放这里。
- `projects/`：正在推进的工作项目、专题任务、阶段性研究。
- `outputs/`：对外可用成品，如通知、讲话稿、汇报、清单、方案等。
- `templates/`：Obsidian、Claudian、Codex 共用模板。

## 查询原则

1. 先读 `wiki/index.md`，确定已有知识范围。
2. 优先从 `wiki/` 已整理内容回答。
3. `wiki/` 不足时，再回看 `raw/` 或相关工作目录。
4. 如果从 `raw/` 得到可复用知识，应在用户同意或任务需要时整理回 `wiki/`。
5. 回答应尽量标注来自哪个页面或文件，便于追溯。

## 摄取原则

当用户要求“摄取”“整理到知识库”“处理资料”时：

1. 不覆盖原始资料。
2. 为每个来源创建或更新 `wiki/来源/` 页面。
3. 提炼关键观点、事实、案例、概念、实体。
4. 使用 Obsidian 双链，例如 `[[wiki/概念/知识管理系统]]`。
5. 按 `CLAUDE.md` 的创建时机规则维护实体页、概念页、对比页。
6. 更新 `wiki/index.md` 和追加 `wiki/log.md`。

## 输出原则

- 给外部使用的正式材料，保存到 `outputs/`。
- 项目过程材料、思路、版本记录，保存到 `projects/`。
- 生成材料要优先复用 `wiki/` 中已经沉淀的观点和表达。
- 不把临时草稿直接混入 `wiki/`，除非它已经具备长期复用价值。

## 与 Claudian 协同

- Claudian 适合在当前 Obsidian 页面里做轻量处理。
- Codex 适合跨目录读取、批量比对、生成多个文件、检查链接和维护索引。
- 同一任务如果涉及多个文件、多个来源、多个输出版本，优先交给 Codex。

## 常用 Codex 指令示例

- `请摄取 raw/xxx.md，生成来源页并更新 wiki/index.md 和 wiki/log.md`
- `请基于 wiki/概念/知识管理系统，帮我生成一份工作方案，保存到 outputs/`
- `请检查 wiki 是否有孤儿页、缺页链接、index 统计不一致的问题`
- `请把 01 Inbox/ 里的材料归类，哪些进入 raw，哪些进入 projects`
- `请对 projects/某项目 做阶段复盘，并沉淀可复用经验到 wiki/概念/`
